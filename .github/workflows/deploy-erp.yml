name: Deploy ERP to Vercel

on:
  push:
    branches:
      - main
    paths:
      - "apps/tulie_erp/**"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npx turbo build --filter=tulie_erp

      - name: Deploy to Vercel
        run: |
          cd apps/tulie_erp
          npx vercel --prod --yes --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_ERP_PROJECT_LINK }}
